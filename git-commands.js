// 1. git init (inside project directory) => untuk ngetrack perubahan (warning: untuk first initiate)
// 2. git add index.js => hanya file index.js dikirim sebagai perubahan / git add . => semua file dikirim sebagai perubahan
// 3. git commit -m "Create variable inside index.js"
// 4. git log
// 5. git remote add origin https://github.com/Raihan162/JCWD2302_SCM.git (warning: untuk first initiate only)
// 6. git push origin master

// Untuk membuat branch baru
// 1. git checkout -b Day01_NewVariable (setelah -b nama branchnya bebas)